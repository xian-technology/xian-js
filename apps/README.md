# Apps

User-facing wallet applications no longer live in `xian-js`.

They were split into the sibling `../xian-wallet-browser` repo so the SDK
workspace can stay focused on `@xian/client` and `@xian/provider`.
