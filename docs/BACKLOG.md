# Backlog

This is the stable backlog entrypoint for `xian-js`.

Current themes:

- shared canonicalization test vectors against `xian-py`
- richer typed models for BDS-backed reads
- docs and reference apps in `xian-docs-web`
- multi-wallet browser discovery beyond a single injected provider
- publishable package distribution and release workflow
